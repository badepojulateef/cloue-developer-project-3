apiVersion: v1
kind: ConfigMap
metadata:
    name: env-configmap
data:
    POSTGRES_USERNAME: postgres
    POSTGRES_PASSWORD: postgres
    POSTGRES_HOST: postgres.cuyl4ghwpvfr.us-east-2.rds.amazonaws.com
    POSTGRES_DB: postgres
    AWS_BUCKET: lateefmonotomicro
    AWS_REGION: us-east-2
    AWS_PROFILE: default
    JWT_SECRET: testing
    URL: http://localhost:8100
